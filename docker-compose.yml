version: '3.8'
services:
  app:
    build: .
    container_name: formatador-botconversa
    ports:
      - "8501:8501"
    restart: always
    environment:
      - STREAMLIT_SERVER_HEADLESS=true
      - STREAMLIT_SERVER_ADDRESS=0.0.0.0